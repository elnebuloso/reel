version: 1

properties:
  docker:
    username: foo
    password: bar
    build:
      file: Dockerfile
      context: .
    push:
      - image:1.2.3
      - image:1.2
      - image:1
      - image:latest
